# Spring
spring.application.name=API-GATEWAY
spring.profiles.active=dev

# Eureka Discovery Service
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=localhost

# Cloud
spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true
#spring.cloud.discovery.enabled=true
#management.endpoint.gateway.enabled=true

#spring.config.import="optional:configserver:${CONFIG_SERVER:http://localhost:8080}"
#springdoc.version='@springdoc.version@'
# springdoc.swagger-ui.use-root-path=true

# springdoc.swagger-ui.urls[0].name=movies
# springdoc.swagger-ui.urls[0].url=/v3/api-docs/movies
# springdoc.swagger-ui.urls[1].name=users
# springdoc.swagger-ui.urls[1].url=/v3/api-docs/users
# springdoc.cache.disabled=true

springdoc.swagger-ui.path=/
server.forward-headers-strategy=framework
spring.main.banner-mode='off'
swagger.config.refreshrate=5000